<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.www.mypage.mapper.MyPageMapper">

	<select id="getMyClass" resultType="com.project.www.mypage.dto.ListenClass">
		select m.id,c.class_name,
		c.image, d.title from
		listen_class lc, class c, dtail_kategorie d, member m
		where m.id = lc.id
		and lc.class_id = c.class_id
		and c.dtail_num = d.dtail_num
		and m.id = 1;
	</select>

</mapper>
